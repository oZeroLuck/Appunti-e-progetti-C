#ifndef nsqrt_h
#define nsqrt_h
/**
 * (Per Doxygen)
 *Calcolo della radice quadrata di un numero con risoluzione di Newton.
 *Utilizziamo param che è una keyword per definire i parametri:
 *@param x valore di input
 *@param epsilon precisione di calcolo
 *Per rappresentare i valori di ritorno usiamo la keyword return:
 *@return il valore della rdice quadrata di x
**/
double nsqrt(double x, double epsilon);

#endif
